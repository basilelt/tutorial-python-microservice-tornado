python-type-check:
  stage: test
  image: ${CI_REGISTRY_IMAGE}:${TAG}
  variables:
    TAG: $CI_COMMIT_REF_SLUG
  rules:
    - if: '$CI_COMMIT_BRANCH'
    - if: '$CI_COMMIT_TAG'
      variables:  
        TAG: $CI_COMMIT_TAG
  script:
    - mypy ./addrservice ./tests

